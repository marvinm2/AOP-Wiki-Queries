# title: Count stressors
# description: Returns the total number of Stressors (chemicals) in the dataset.
# category: Metadata

SELECT (COUNT(DISTINCT ?stressor) AS ?total_Stressors)
WHERE {
  ?stressor a nci:C54571 .
}
